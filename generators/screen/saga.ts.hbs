import { takeLatest } from 'redux-saga/effects';

import { {{ upperCase name }}_START } from './constants';

function* fetch{{ camelCase name }}Data() {
    try {
        console.log('hi');
    } catch (error) {
        console.log(error);
    }
}
export function* {{ camelCase name }}Saga() {
    // Take Last Action Only
    yield takeLatest({{ upperCase name }}_START, fetch{{ camelCase name }}Data);
}